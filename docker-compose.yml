version: '2'
services:
  web:
    build: .
    environment:
      - APP_SETTINGS=config.DevelopmentConfig
      - DATABASE_URL=sqlite:///timewebsite.db
    volumes:
      - .:/usr/src/app
    command: python run.py
